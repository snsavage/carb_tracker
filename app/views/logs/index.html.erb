<h1>Food Logs</h1>

<div class="chart">
  <%# <%= column_chart @log_data if @log_data.present? %1> %>
  <% if @log_data.present? %>
    <%= column_chart @log_data %>
  <% else %>
    <p>You haven't logged any foods.  Why not start today!
    <%= link_to "Log Foods for Today", today_user_logs_path(current_user) %></p>
  <% end %>
</div>

<%= render partial: 'logs', collection: @logs %>

